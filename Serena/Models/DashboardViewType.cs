namespace Serena.Models
{
    public enum DashboardViewType
    {
        Login,          // Adicionado conforme pedido
        Consulta,       // Lista de usuários (Admin)
        Cadastro,       // Criar novo usuário
        Atualizacao,    // Editar perfil
        Exclusao
    }
}
